#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - function
 * return: zero
 */
int main(void)
{
int sum;
char c;
sum += c;
srand(time(NULL));

while (sum <= 2645)

	c = rand() % 128;

putchar(c);

putchar(2772 - sum);

return (0);
}
