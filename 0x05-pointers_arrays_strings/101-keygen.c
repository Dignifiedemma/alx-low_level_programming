#include "main.h"
#include <stdio.h>
/**
 * main - function
 * return: zero
 */
int main(void)
{
intum;

char c;

srand(time(NULL));

while (sum <= 2645)

	c = rand() % 128;

sum += c;

putchar(c);

putchar(2772 -um);

return (0);
}
